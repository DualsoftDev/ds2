<UserControl x:Class="Promaker.Controls.ConditionSectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:help="clr-namespace:Promaker.Help"
             x:Name="Root">
    <UserControl.Resources>
        <!-- ApiCall 행 템플릿 -->
        <DataTemplate x:Key="ApiCallRowTemplate">
            <Grid Margin="0,1"
                  Background="Transparent"
                  Cursor="Hand"
                  MouseLeftButtonDown="ApiCallRow_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0"
                        Content="x"
                        Style="{DynamicResource InlineListButton}"
                        Command="{Binding RemoveConditionApiCallCommand, ElementName=Root}"
                        CommandParameter="{Binding}"/>
                <TextBlock Grid.Column="1"
                           Text="{Binding ApiDefDisplayName}"
                           FontSize="10"
                           VerticalAlignment="Center"
                           Margin="4,0"/>
                <TextBlock Grid.Column="2"
                           Text="{Binding OutputSpecText}"
                           FontSize="10"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource AccentBrush}"
                           Margin="4,0"/>
                <Button Grid.Column="3"
                        Content="✎"
                        ToolTip="Edit OutputSpec"
                        Style="{DynamicResource InlineListButton}"
                        Command="{Binding EditConditionApiCallSpecCommand, ElementName=Root}"
                        CommandParameter="{Binding}"/>
            </Grid>
        </DataTemplate>

        <!-- 재귀 조건 항목 템플릿 -->
        <DataTemplate x:Key="ConditionItemTemplate">
            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="4,4">
                <StackPanel>
                    <!-- 조건 헤더: x, OR, Rising, + -->
                    <StackPanel Orientation="Horizontal">
                        <Button Content="x"
                                Style="{DynamicResource InlineListButton}"
                                Command="{Binding RemoveConditionCommand, ElementName=Root}"
                                CommandParameter="{Binding}"/>
                        <CheckBox Content="OR"
                                  IsChecked="{Binding IsOR, Mode=OneWay}"
                                  Margin="4,0"
                                  VerticalAlignment="Center"
                                  Command="{Binding ToggleConditionIsOrCommand, ElementName=Root}"
                                  CommandParameter="{Binding}"/>
                        <CheckBox Content="Rising"
                                  IsChecked="{Binding IsRising, Mode=OneWay}"
                                  Margin="4,0"
                                  VerticalAlignment="Center"
                                  Command="{Binding ToggleConditionIsRisingCommand, ElementName=Root}"
                                  CommandParameter="{Binding}"/>
                        <Button Content="+"
                                ToolTip="하위 그룹 추가"
                                Style="{DynamicResource InlineListButton}"
                                Margin="4,0"
                                Command="{Binding AddChildConditionCommand, ElementName=Root}"
                                CommandParameter="{Binding}"/>
                    </StackPanel>
                    <!-- ApiCall 목록 -->
                    <ItemsControl ItemsSource="{Binding Items}" Margin="8,2,0,0"
                                  ItemTemplate="{StaticResource ApiCallRowTemplate}"/>
                    <!-- 재귀 하위 조건 -->
                    <ItemsControl ItemsSource="{Binding Children}" Margin="16,2,0,0"
                                  ItemTemplate="{DynamicResource ConditionItemTemplate}"/>
                    <!-- 클릭하여 조건 추가 힌트 -->
                    <TextBlock Text="조건을 추가하려면 여기를 클릭 하세요"
                               FontSize="10"
                               FontStyle="Italic"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               Margin="8,4,0,2"
                               Cursor="Hand"
                               MouseLeftButtonDown="AddConditionHint_Click"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Border BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            Margin="0,2,0,4"
            Background="{DynamicResource TertiaryBackgroundBrush}">
        <StackPanel>
            <Border Background="{DynamicResource SecondaryBackgroundBrush}" Padding="4,3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="{Binding HeaderText, ElementName=Root}"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                        <Button Style="{DynamicResource HelpButton}"
                                Command="{x:Static help:HelpNavigator.NavigateCommand}"
                                CommandParameter="{Binding HelpTopic, ElementName=Root}"/>
                    </StackPanel>
                    <Button Grid.Column="2"
                            Content="+"
                            ToolTip="{Binding AddToolTip, ElementName=Root}"
                            Style="{DynamicResource InlineListButton}"
                            Command="{Binding AddCommand, ElementName=Root}"
                            CommandParameter="{Binding AddCommandParameter, ElementName=Root}"/>
                </Grid>
            </Border>
            <ItemsControl ItemsSource="{Binding ItemsSource, ElementName=Root}"
                          ItemTemplate="{StaticResource ConditionItemTemplate}"/>
        </StackPanel>
    </Border>
</UserControl>
