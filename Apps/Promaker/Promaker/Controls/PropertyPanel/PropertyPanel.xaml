<UserControl x:Class="Promaker.Controls.PropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Promaker.ViewModels"
             xmlns:ctrl="clr-namespace:Promaker.Controls"
             xmlns:conv="clr-namespace:Promaker.Presentation"
             xmlns:core="clr-namespace:Ds2.UI.Core;assembly=Ds2.UI.Core"
             xmlns:help="clr-namespace:Promaker.Help"
             x:Name="PanelRoot">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="InlineListButton" TargetType="Button" BasedOn="{StaticResource DarkButton}">
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="MinWidth" Value="24"/>
            <Setter Property="Height" Value="22"/>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource PanelContainer}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Style="{StaticResource PanelHeader}">
                <Grid>
                    <TextBlock Text="Properties" FontWeight="SemiBold" FontSize="12"/>
                    <Button Style="{StaticResource HelpButton}" HorizontalAlignment="Right"
                            Command="{x:Static help:HelpNavigator.NavigateCommand}"
                            CommandParameter="properties"/>
                </Grid>
            </Border>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="8">
                    <TextBlock Text="{Binding SelectedNode.EntityType, StringFormat='Type: {0}', FallbackValue='No selection'}"
                               FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding SelectedNode.Name, StringFormat='Name: {0}'}"
                               FontSize="13" FontWeight="SemiBold" Margin="0,0,0,12"/>

                    <TextBlock Text="Name" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,2"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="NameEditor" Text="{Binding NameEditorText, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="6,4" FontSize="12"
                                 KeyDown="NameEditor_KeyDown"/>
                        <Button Grid.Column="1" Content="✓" Style="{StaticResource DarkButton}"
                                Margin="4,0,0,0" Padding="8,4"
                                Command="{Binding ApplyNameCommand}"
                                IsEnabled="{Binding IsNameDirty}"/>
                    </Grid>

                    <Separator Margin="0,12"/>

                    <TextBlock Text="Position" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,2"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="X:" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SelectedNode.X, StringFormat='{}{0:0}'}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Y:" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SelectedNode.Y, StringFormat='{}{0:0}'}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Separator Margin="0,12"/>

                    <StackPanel Visibility="{Binding IsSystemSelected, Converter={StaticResource BoolToVis}}">
                        <Border BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                Margin="0,2,0,8"
                                Background="{DynamicResource TertiaryBackgroundBrush}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0"
                                        Background="{DynamicResource SecondaryBackgroundBrush}"
                                        Padding="4,3">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding SystemApiDefsHeader}"
                                                   FontSize="11"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                                        <Button Grid.Column="1"
                                                Content="+"
                                                ToolTip="Add ApiDef"
                                                Style="{StaticResource InlineListButton}"
                                                Command="{Binding AddSystemApiDefCommand}"/>
                                    </Grid>
                                </Border>

                                <ItemsControl Grid.Row="1"
                                              ItemsSource="{Binding SystemApiDefs}"
                                              MinHeight="40" MaxHeight="200">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="4,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Name}"
                                                           VerticalAlignment="Center"
                                                           FontSize="11"/>
                                                <Button Grid.Column="1"
                                                        Content="✎"
                                                        ToolTip="Edit ApiDef"
                                                        Style="{StaticResource InlineListButton}"
                                                        Margin="4,0,0,0"
                                                        Command="{Binding DataContext.EditSystemApiDefCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"/>
                                                <Button Grid.Column="2"
                                                        Content="×"
                                                        ToolTip="Delete ApiDef"
                                                        Style="{StaticResource InlineListButton}"
                                                        Margin="2,0,0,0"
                                                        Foreground="{DynamicResource RedAccentBrush}"
                                                        Command="{Binding DataContext.DeleteSystemApiDefCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                        <Separator Margin="0,4"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsWorkSelected, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{x:Static core:Labels.WorkPeriod}"
                                   FontSize="11"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Margin="0,2,0,2"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding WorkPeriodMs, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                                     Padding="6,4"
                                     FontSize="12"
                                     ToolTip="{x:Static core:Labels.PeriodFormat}"
                                     MaxLength="9">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding ApplyWorkPeriodCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                            <Button Grid.Column="1"
                                    Content="✓"
                                    Style="{StaticResource DarkButton}"
                                    Margin="4,0,0,0"
                                    Padding="8,4"
                                    IsEnabled="{Binding IsWorkPeriodDirty}"
                                    Command="{Binding ApplyWorkPeriodCommand}"/>
                        </Grid>
                        <Separator Margin="0,12"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsCallSelected, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{x:Static core:Labels.TimeoutMs}" FontSize="11"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Margin="0,4,0,2"/>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding CallTimeoutMs, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                                     Padding="6,4">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding ApplyCallTimeoutCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                            <Button Grid.Column="1"
                                    Content="✓"
                                    Style="{StaticResource DarkButton}"
                                    Margin="4,0,0,0"
                                    Padding="10,4"
                                    IsEnabled="{Binding IsCallTimeoutDirty}"
                                    Command="{Binding ApplyCallTimeoutCommand}"/>
                        </Grid>
                        <Border BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                Margin="0,2,0,8"
                                Background="{DynamicResource TertiaryBackgroundBrush}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0"
                                        Background="{DynamicResource SecondaryBackgroundBrush}"
                                        Padding="4,3">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding CallApiCallsHeader}"
                                                   FontSize="11"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                                        <Button Grid.Column="1"
                                                Content="+"
                                                ToolTip="Add ApiCall"
                                                Style="{StaticResource InlineListButton}"
                                                Command="{Binding AddCallApiCallCommand}"/>
                                    </Grid>
                                </Border>

                                <DataGrid Grid.Row="1"
                                          ItemsSource="{Binding CallApiCalls}"
                                          SelectedItem="{Binding SelectedCallApiCall}"
                                          AutoGenerateColumns="False"
                                          CanUserAddRows="False"
                                          CanUserDeleteRows="False"
                                          HeadersVisibility="Column"
                                          GridLinesVisibility="Horizontal"
                                          HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                                          VerticalGridLinesBrush="{DynamicResource BorderBrush}"
                                          BorderThickness="0"
                                          RowHeaderWidth="0"
                                          MinHeight="160"
                                          MaxHeight="240"
                                          Background="{DynamicResource TertiaryBackgroundBrush}"
                                          Foreground="{DynamicResource PrimaryTextBrush}">
                                    <DataGrid.ColumnHeaderStyle>
                                        <Style TargetType="DataGridColumnHeader">
                                            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                            <Setter Property="Padding" Value="4,3"/>
                                            <Setter Property="FontSize" Value="11"/>
                                        </Style>
                                    </DataGrid.ColumnHeaderStyle>
                                    <DataGrid.RowStyle>
                                        <Style TargetType="DataGridRow">
                                            <Setter Property="Background" Value="{DynamicResource TertiaryBackgroundBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGrid.RowStyle>
                                    <DataGrid.CellStyle>
                                        <Style TargetType="DataGridCell">
                                            <Setter Property="Background" Value="{DynamicResource TertiaryBackgroundBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Padding" Value="2"/>
                                        </Style>
                                    </DataGrid.CellStyle>
                                    <DataGrid.Columns>
                                        <DataGridTemplateColumn Width="32">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="x"
                                                            Style="{StaticResource InlineListButton}"
                                                            Margin="0,1,0,1"
                                                            Command="{Binding DataContext.RemoveCallApiCallCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                            CommandParameter="{Binding}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTextColumn Header="{x:Static core:Labels.ApiDef}" Width="1.5*"
                                                            Binding="{Binding ApiDefDisplayName, Mode=OneWay}"
                                                            IsReadOnly="True">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Padding" Value="4,2"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>

                                        <DataGridTemplateColumn Header="{x:Static core:Labels.OutTag}" Width="*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBox Text="{Binding OutputTagName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                             Padding="4,2"
                                                             Margin="0,1,0,1"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Header="{x:Static core:Labels.OutAddress}" Width="*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBox Text="{Binding OutputAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                             Padding="4,2"
                                                             Margin="0,1,0,1"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Header="{x:Static core:Labels.InTag}" Width="*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBox Text="{Binding InputTagName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                             Padding="4,2"
                                                             Margin="0,1,0,1"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Header="{x:Static core:Labels.InAddress}" Width="*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBox Text="{Binding InputAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                             Padding="4,2"
                                                             Margin="0,1,0,1"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Header="{x:Static core:Labels.OutSpec}" Width="1.2*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBox Text="{Binding ValueSpecText, Mode=OneWay}"
                                                             IsReadOnly="True"
                                                             Padding="4,2"
                                                             Margin="0,1,0,1"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Header="{x:Static core:Labels.InSpec}" Width="1.2*">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,1,0,1">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBox Grid.Column="0"
                                                                 Text="{Binding InputValueSpecText, Mode=OneWay}"
                                                                 IsReadOnly="True"
                                                                 Padding="4,2"/>
                                                        <Button Grid.Column="1"
                                                                Content="✎"
                                                                Style="{StaticResource InlineListButton}"
                                                                Margin="4,0,0,0"
                                                                Command="{Binding DataContext.EditCallApiCallSpecCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                                CommandParameter="{Binding}"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>

                                        <DataGridTemplateColumn Width="32">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="✓"
                                                            Style="{StaticResource InlineListButton}"
                                                            Margin="0,1,0,1"
                                                            IsEnabled="{Binding IsDirty}"
                                                            Command="{Binding DataContext.UpdateCallApiCallCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                            CommandParameter="{Binding}"/>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Grid>
                        </Border>

                        <ItemsControl ItemsSource="{Binding ConditionSections}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ConditionSectionItem}">
                                    <ctrl:ConditionSectionControl
                                        HeaderText="{Binding Header}"
                                        HelpTopic="{Binding HelpTopic}"
                                        AddToolTip="{Binding AddToolTip}"
                                        ItemsSource="{Binding Conditions}"
                                        AddCommand="{Binding DataContext.AddConditionCommand, ElementName=PanelRoot}"
                                        RemoveConditionCommand="{Binding DataContext.RemoveCallConditionCommand, ElementName=PanelRoot}"
                                        ToggleConditionIsOrCommand="{Binding DataContext.ToggleConditionIsORCommand, ElementName=PanelRoot}"
                                        ToggleConditionIsRisingCommand="{Binding DataContext.ToggleConditionIsRisingCommand, ElementName=PanelRoot}"
                                        AddConditionApiCallCommand="{Binding DataContext.AddConditionApiCallCommand, ElementName=PanelRoot}"
                                        RemoveConditionApiCallCommand="{Binding DataContext.RemoveConditionApiCallCommand, ElementName=PanelRoot}"
                                        EditConditionApiCallSpecCommand="{Binding DataContext.EditConditionApiCallSpecCommand, ElementName=PanelRoot}"
                                        NavigateConditionApiCallCommand="{Binding DataContext.NavigateConditionApiCallCommand, ElementName=PanelRoot}"
                                        AddChildConditionCommand="{Binding DataContext.AddChildConditionCommand, ElementName=PanelRoot}"
                                        AddCommandParameter="{Binding ConditionType}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Separator Margin="0,12"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsDebugBuild, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="ID" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,2"/>
                        <TextBox Text="{Binding SelectedNode.Id, Mode=OneWay}" IsReadOnly="True"
                                 Padding="6,4" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
