<UserControl x:Class="Promaker.Controls.PropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Promaker.ViewModels"
             xmlns:ctrl="clr-namespace:Promaker.Controls"
             xmlns:conv="clr-namespace:Promaker.Presentation"
             xmlns:core="clr-namespace:Ds2.Store;assembly=Ds2.Store"
             xmlns:help="clr-namespace:Promaker.Help"
             x:Name="PanelRoot">
    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="InlineListButton" TargetType="Button" BasedOn="{StaticResource DarkButton}">
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="MinWidth" Value="24"/>
            <Setter Property="Height" Value="22"/>
        </Style>
    </UserControl.Resources>

    <Border Style="{StaticResource PanelContainer}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Style="{StaticResource PanelHeader}">
                <Grid>
                    <TextBlock Text="Properties" FontWeight="SemiBold" FontSize="12"/>
                    <Button Style="{StaticResource HelpButton}" HorizontalAlignment="Right"
                            Command="{x:Static help:HelpNavigator.NavigateCommand}"
                            CommandParameter="properties"/>
                </Grid>
            </Border>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="8">
                    <TextBlock Text="{Binding SelectedNode.EntityType, StringFormat='Type: {0}', FallbackValue='선택 없음'}"
                               FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding SelectedNode.Name, StringFormat='Name: {0}'}"
                               FontSize="13" FontWeight="SemiBold" Margin="0,0,0,12"/>

                    <TextBlock Text="Name" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,2"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Text="{Binding NamePrefix}"
                                   FontSize="12" FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,2,0"/>
                        <TextBox x:Name="NameEditor" Grid.Column="1"
                                 Text="{Binding NameEditorText, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="6,4" FontSize="12"
                                 KeyDown="NameEditor_KeyDown"/>
                        <Button Grid.Column="2" Content="✓" Style="{StaticResource DarkButton}"
                                Margin="4,0,0,0" Padding="8,4"
                                Command="{Binding ApplyNameCommand}"
                                IsEnabled="{Binding IsNameDirty}"/>
                    </Grid>

                    <Separator Margin="0,12"/>

                    <TextBlock Text="Position" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,2"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="X:" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SelectedNode.X, StringFormat='{}{0:0}'}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Y:" Margin="0,0,4,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding SelectedNode.Y, StringFormat='{}{0:0}'}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Separator Margin="0,12"/>

                    <StackPanel Visibility="{Binding IsSystemSelected, Converter={StaticResource BoolToVis}}">
                        <Border BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                Margin="0,2,0,8"
                                Background="{DynamicResource TertiaryBackgroundBrush}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0"
                                        Background="{DynamicResource SecondaryBackgroundBrush}"
                                        Padding="4,3">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding SystemApiDefsHeader}"
                                                   FontSize="11"
                                                   VerticalAlignment="Center"
                                                   Foreground="{DynamicResource SecondaryTextBrush}"/>
                                        <Button Grid.Column="1"
                                                Content="+"
                                                ToolTip="Add ApiDef"
                                                Style="{StaticResource InlineListButton}"
                                                Command="{Binding AddSystemApiDefCommand}"/>
                                    </Grid>
                                </Border>

                                <ItemsControl Grid.Row="1"
                                              ItemsSource="{Binding SystemApiDefs}"
                                              MinHeight="40" MaxHeight="200">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="4,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0"
                                                           Text="{Binding Name}"
                                                           VerticalAlignment="Center"
                                                           FontSize="11"/>
                                                <Button Grid.Column="1"
                                                        Content="✎"
                                                        ToolTip="Edit ApiDef"
                                                        Style="{StaticResource InlineListButton}"
                                                        Margin="4,0,0,0"
                                                        Command="{Binding DataContext.EditSystemApiDefCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"/>
                                                <Button Grid.Column="2"
                                                        Content="×"
                                                        ToolTip="Delete ApiDef"
                                                        Style="{StaticResource InlineListButton}"
                                                        Margin="2,0,0,0"
                                                        Foreground="{DynamicResource RedAccentBrush}"
                                                        Command="{Binding DataContext.DeleteSystemApiDefCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                        CommandParameter="{Binding}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                        <Separator Margin="0,4"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsWorkSelected, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{x:Static core:Labels.WorkPeriod}"
                                   FontSize="11"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Margin="0,2,0,2"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding WorkPeriodMs, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                                     Padding="6,4"
                                     FontSize="12"
                                     ToolTip="{x:Static core:Labels.PeriodFormat}"
                                     MaxLength="9">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding ApplyWorkPeriodCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                            <Button Grid.Column="1"
                                    Content="✓"
                                    Style="{StaticResource DarkButton}"
                                    Margin="4,0,0,0"
                                    Padding="8,4"
                                    IsEnabled="{Binding IsWorkPeriodDirty}"
                                    Command="{Binding ApplyWorkPeriodCommand}"/>
                        </Grid>
                        <TextBlock Text="{Binding DeviceDurationHint}"
                                   FontSize="11"
                                   Foreground="{DynamicResource AccentTextBrush}"
                                   Visibility="{Binding HasDeviceDuration, Converter={StaticResource BoolToVis}}"
                                   Margin="0,2,0,0"/>
                        <Separator Margin="0,12"/>
                        <TextBlock Text="TokenRole"
                                   FontSize="11"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Margin="0,2,0,2"/>
                        <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                            <CheckBox Content="Source"
                                      IsChecked="{Binding IsTokenSource}"
                                      FontSize="12" Margin="0,0,16,0"/>
                            <CheckBox Content="Ignore"
                                      IsChecked="{Binding IsTokenIgnore}"
                                      FontSize="12"/>
                        </StackPanel>
                        <TextBlock Text="연결된 TokenSpec"
                                   FontSize="11"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Margin="0,8,0,2"
                                   Visibility="{Binding HasLinkedTokenSpec, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding LinkedTokenSpecLabel}"
                                   FontSize="12"
                                   Foreground="{DynamicResource TokenOrangeBrush}"
                                   Margin="0,2,0,0"
                                   Visibility="{Binding HasLinkedTokenSpec, Converter={StaticResource BoolToVis}}"/>
                        <Separator Margin="0,12"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsCallSelected, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="{x:Static core:Labels.TimeoutMs}" FontSize="11"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   Margin="0,4,0,2"/>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding CallTimeoutMs, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                                     Padding="6,4">
                                <TextBox.InputBindings>
                                    <KeyBinding Key="Enter" Command="{Binding ApplyCallTimeoutCommand}"/>
                                </TextBox.InputBindings>
                            </TextBox>
                            <Button Grid.Column="1"
                                    Content="✓"
                                    Style="{StaticResource DarkButton}"
                                    Margin="4,0,0,0"
                                    Padding="10,4"
                                    IsEnabled="{Binding IsCallTimeoutDirty}"
                                    Command="{Binding ApplyCallTimeoutCommand}"/>
                        </Grid>
                        <ctrl:ApiCallsGridControl DataContext="{Binding}"/>

                        <ItemsControl ItemsSource="{Binding ConditionSections}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type vm:ConditionSectionItem}">
                                    <ctrl:ConditionSectionControl
                                        HeaderText="{Binding Header}"
                                        HelpTopic="{Binding HelpTopic}"
                                        AddToolTip="{Binding AddToolTip}"
                                        ItemsSource="{Binding Conditions}"
                                        AddCommand="{Binding DataContext.AddConditionCommand, ElementName=PanelRoot}"
                                        RemoveConditionCommand="{Binding DataContext.RemoveCallConditionCommand, ElementName=PanelRoot}"
                                        EditConditionsCommand="{Binding DataContext.EditConditionsCommand, ElementName=PanelRoot}"
                                        EditConditionsParameter="{Binding}"
                                        DropCallCommand="{Binding DataContext.DropCallToConditionSectionCommand, ElementName=PanelRoot}"
                                        AddCommandParameter="{Binding ConditionType}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Separator Margin="0,12"/>
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsDebugBuild, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="ID" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,2"/>
                        <TextBox Text="{Binding SelectedNode.Id, Mode=OneWay}" IsReadOnly="True"
                                 Padding="6,4" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
