<Window x:Class="Promaker.Dialogs.MultiDeviceCallMigrationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Call 구조 마이그레이션"
        Width="900" Height="560"
        MinWidth="680" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Background="{DynamicResource SecondaryBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Grid Margin="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="B안 — Call 당 1개 FB 호출 규칙 위반 Call 목록"
                       FontSize="15" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       Margin="0,0,0,6"/>

            <TextBlock Grid.Row="1"
                       Text="분할: 이종 Device Call 을 Device 별로 복제 (ApiCalls 분배, arrows 는 유지하지 않음). 삭제: ApiCalls 가 비어있는 Call 제거."
                       FontSize="11"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,10"/>

            <DataGrid Grid.Row="2"
                      x:Name="RowsGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      GridLinesVisibility="Horizontal"
                      BorderBrush="{DynamicResource BorderBrush}"
                      Background="{DynamicResource SecondaryBackgroundBrush}"
                      Foreground="{DynamicResource PrimaryTextBrush}"
                      FontSize="12">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Header="적용" Width="60"
                                            Binding="{Binding Selected, UpdateSourceTrigger=PropertyChanged}"/>
                    <DataGridTextColumn Header="Flow"    Width="120" IsReadOnly="True" Binding="{Binding FlowName}"/>
                    <DataGridTextColumn Header="Work"    Width="140" IsReadOnly="True" Binding="{Binding WorkName}"/>
                    <DataGridTextColumn Header="Call"    Width="180" IsReadOnly="True" Binding="{Binding CallName}"/>
                    <DataGridTextColumn Header="사유"    Width="*"   IsReadOnly="True" Binding="{Binding Reason}"/>
                    <DataGridTextColumn Header="Device수" Width="80"  IsReadOnly="True" Binding="{Binding DeviceCount}"/>
                    <DataGridTextColumn Header="Api수"   Width="70"  IsReadOnly="True" Binding="{Binding ApiCallCount}"/>
                </DataGrid.Columns>
            </DataGrid>

            <TextBlock Grid.Row="3"
                       x:Name="StatusText"
                       FontSize="11"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       TextWrapping="Wrap"
                       Margin="0,10,0,0"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="모두 선택"     Padding="10,4" Margin="0,0,4,0" Click="SelectAll_Click"/>
                <Button Content="모두 해제"     Padding="10,4" Margin="0,0,12,0" Click="SelectNone_Click"/>
                <Button Content="적용 (분할/삭제)" Padding="12,4" Margin="0,0,4,0"
                        Click="Apply_Click" IsDefault="True"/>
                <Button Content="닫기" Padding="12,4" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
