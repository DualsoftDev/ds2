<Window x:Class="Promaker.Dialogs.WorkPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Work 선택"
        Width="420"
        Height="460"
        MinWidth="320"
        MinHeight="320"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="CanResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Border Background="{DynamicResource SecondaryBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBox Grid.Row="0"
                     x:Name="SearchBox"
                     Height="26"
                     FontSize="12"
                     VerticalContentAlignment="Center"
                     ToolTip="Work 이름 검색"
                     TextChanged="SearchBox_TextChanged"/>

            <CheckBox Grid.Row="2"
                      x:Name="SourceOnlyCheckBox"
                      Content="Source Work만 보기"
                      Foreground="{DynamicResource PrimaryTextBrush}"
                      FontSize="11"
                      Checked="SourceOnly_Changed"
                      Unchecked="SourceOnly_Changed"/>

            <Border Grid.Row="4"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="3"
                    Background="{DynamicResource TertiaryBackgroundBrush}">
                <ListBox x:Name="WorkList"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{DynamicResource PrimaryTextBrush}"
                         SelectionMode="Single"
                         MouseDoubleClick="WorkList_MouseDoubleClick"
                         SelectionChanged="WorkList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="2,1">
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                <TextBlock Text="  (Source)"
                                           Foreground="{DynamicResource AccentBrush}"
                                           FontSize="10"
                                           VerticalAlignment="Center"
                                           Visibility="{Binding IsSource, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <Grid Grid.Row="6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock VerticalAlignment="Center"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="11">
                    <Run Text="총 "/>
                    <Run x:Name="CountRun" Text="0" FontWeight="Bold"/>
                    <Run Text="개"/>
                </TextBlock>

                <Button Grid.Column="1"
                        x:Name="OkButton"
                        Content="적용"
                        Style="{StaticResource DarkButton}"
                        MinWidth="76"
                        IsDefault="True"
                        IsEnabled="False"
                        Click="OK_Click"/>

                <Button Grid.Column="3"
                        Content="취소"
                        Style="{StaticResource DarkButton}"
                        MinWidth="76"
                        IsCancel="True"/>
            </Grid>
        </Grid>
    </Border>
</Window>
