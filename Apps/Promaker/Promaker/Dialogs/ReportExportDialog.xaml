<Window x:Class="Promaker.Dialogs.ReportExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="시뮬레이션 리포트 출력"
        Width="380" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}"
        ShowInTaskbar="False">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="20">
        <TextBlock Text="출력 형식" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <RadioButton x:Name="RadioCsv"   GroupName="Fmt" Content="CSV (.csv)" IsChecked="True"/>
        <RadioButton x:Name="RadioExcel" GroupName="Fmt" Content="Excel (.xlsx)"/>
        <RadioButton x:Name="RadioHtml"  GroupName="Fmt" Content="HTML (.html)"/>

        <Separator Margin="0,12,0,8"/>

        <CheckBox x:Name="OpenAfterExport" Content="출력 후 파일 열기" IsChecked="True"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="출력" Click="Ok_Click" IsDefault="True"
                    Style="{StaticResource DarkButton}"
                    Padding="16,5" Margin="0,0,8,0"/>
            <Button Content="취소" IsCancel="True" Padding="16,5"
                    Style="{StaticResource DarkButton}"/>
        </StackPanel>
    </StackPanel>
</Window>
