<Window x:Class="Promaker.Dialogs.PskEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LightHouse PSK 입력" Height="200" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Window.Resources>
        <!-- ApplicationSettingsDialog 와 동일 패턴 — TextBox / PasswordBox default light style 차단 (dark 정합). -->
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>
        <Style TargetType="{x:Type PasswordBox}">
            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="ServiceNameText" Grid.Row="0"
                   FontSize="12" FontWeight="SemiBold"
                   Foreground="{DynamicResource PrimaryTextBrush}"
                   Margin="0,0,0,8" TextWrapping="Wrap"/>
        <TextBlock Grid.Row="1"
                   Text="Pre-Shared Key (DPAPI 로 암호화되어 저장됩니다)"
                   FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"
                   Margin="0,0,0,4"/>
        <PasswordBox x:Name="PskBox" Grid.Row="2" Padding="6,4" FontFamily="Consolas"
                     PasswordChanged="PskBox_PasswordChanged"/>
        <TextBlock x:Name="HintText" Grid.Row="3"
                   FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"
                   FontStyle="Italic" TextWrapping="Wrap" Margin="0,4,0,0"
                   Text="기존 PSK 가 박제된 경우 빈 값으로 저장하면 PSK 제거됩니다."/>
        <Grid Grid.Row="4" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="2" Content="OK" IsDefault="True"
                    Style="{StaticResource DarkButton}"
                    Padding="20,4" Click="Ok_Click"/>
            <Button Grid.Column="4" Content="Cancel" IsCancel="True"
                    Style="{StaticResource DarkButton}"
                    Padding="20,4"/>
        </Grid>
    </Grid>
</Window>
