<Window x:Class="Promaker.Windows.NewCustomModelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="커스텀 SystemType 빠른 등록"
        Width="480" Height="460"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{DynamicResource PrimaryBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="프로젝트에 정의되지 않은 SystemType의 3D 메타데이터만 빠르게 등록합니다."
                   Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="SystemType 이름" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="NameInput" Height="30" FontSize="13"
                     Background="{DynamicResource TertiaryBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}"
                     Padding="8,4" TextChanged="OnInputChanged"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,4">
            <TextBlock Text="ApiDef 이름 목록" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="13" FontWeight="SemiBold"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBlock Text="(예: Fwd, Bwd / Open, Close)" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="3" Background="{DynamicResource TertiaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Padding="6">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="ApiDefList">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                                         Height="28" FontSize="13"
                                         Background="{DynamicResource PrimaryBackgroundBrush}" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}"
                                         Padding="8,2" TextChanged="ApiDefRow_TextChanged"/>
                                <Button Grid.Column="1" Content="✕" Width="28" Height="28" Margin="6,0,0,0"
                                        Background="{DynamicResource SecondaryBackgroundBrush}" Foreground="{DynamicResource SecondaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}"
                                        FontSize="12" Cursor="Hand"
                                        Click="RemoveApiDef_Click" Tag="{Binding}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Button Grid.Row="4" Content="+ ApiDef 추가" Click="AddApiDef_Click"
                HorizontalAlignment="Left" Margin="0,8,0,0"
                Background="{DynamicResource SecondaryBackgroundBrush}" Foreground="{DynamicResource SecondaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}"
                Padding="10,4" FontSize="12" Cursor="Hand"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="OkButton" Content="확인" Click="Ok_Click" Width="80" Height="32"
                    Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0"
                    FontSize="13" FontWeight="SemiBold" Cursor="Hand" IsEnabled="False"
                    Margin="0,0,8,0"/>
            <Button Content="취소" Click="Cancel_Click" Width="80" Height="32"
                    Background="{DynamicResource SecondaryBackgroundBrush}" Foreground="{DynamicResource SecondaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}"
                    FontSize="13" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
