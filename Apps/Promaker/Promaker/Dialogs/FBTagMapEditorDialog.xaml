<Window x:Class="Promaker.Dialogs.FBTagMapEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FBTagMap 편집"
        Width="700" Height="520"
        MinWidth="500" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Border Background="{DynamicResource SecondaryBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 시스템 정보 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,10">
                <TextBlock x:Name="SystemNameLabel"
                           FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           Margin="0,0,0,4"/>
                <TextBlock Text="이 시스템의 FB 타입과 포트별 TagPattern을 설정합니다."
                           FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"/>
            </StackPanel>

            <!-- FB 타입 선택 -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="FB 타입"
                           FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"
                           VerticalAlignment="Center"/>
                <ComboBox Grid.Column="2" x:Name="FBTypeComboBox"
                          Padding="6,4"
                          SelectionChanged="FBTypeComboBox_SelectionChanged"/>
                <Button Grid.Column="4" Content="포트 자동 채우기"
                        Style="{StaticResource DarkButton}"
                        Padding="10,4"
                        Click="AutoFillPorts_Click"
                        ToolTip="선택된 FB 타입의 포트 정의를 로드합니다"/>
            </Grid>

            <!-- 포트 그리드 -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="포트 매핑"
                               FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button Content="행 추가" Style="{StaticResource DarkButton}"
                            Padding="8,2" Margin="0,0,4,0"
                            Click="AddRow_Click"/>
                    <Button Content="행 삭제" Style="{StaticResource DarkButton}"
                            Padding="8,2"
                            Click="RemoveRow_Click"/>
                </StackPanel>

                <DataGrid Grid.Row="1" x:Name="PortGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserSortColumns="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          BorderBrush="{DynamicResource BorderBrush}"
                          Background="{DynamicResource SecondaryBackgroundBrush}"
                          RowBackground="{DynamicResource SecondaryBackgroundBrush}"
                          AlternatingRowBackground="{DynamicResource PrimaryBackgroundBrush}"
                          Foreground="{DynamicResource PrimaryTextBrush}"
                          FontSize="12"
                          SelectionMode="Single"
                          SelectionUnit="FullRow">
                    <DataGrid.Columns>
                        <!-- FBPort -->
                        <DataGridTextColumn Header="FBPort" Width="120"
                                            Binding="{Binding FBPort, UpdateSourceTrigger=PropertyChanged}"/>
                        <!-- Direction -->
                        <DataGridTemplateColumn Header="Direction" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Direction}" Padding="4,2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <ComboBox SelectedItem="{Binding Direction, UpdateSourceTrigger=PropertyChanged}"
                                              Padding="4,2">
                                        <ComboBoxItem Content="Input"/>
                                        <ComboBoxItem Content="Output"/>
                                    </ComboBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <!-- DataType -->
                        <DataGridTextColumn Header="DataType" Width="80"
                                            Binding="{Binding DataType, UpdateSourceTrigger=PropertyChanged}"/>
                        <!-- TagPattern -->
                        <DataGridTextColumn Header="TagPattern" Width="*"
                                            Binding="{Binding TagPattern, UpdateSourceTrigger=PropertyChanged}">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                    <Setter Property="FontSize" Value="11"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                            <DataGridTextColumn.EditingElementStyle>
                                <Style TargetType="TextBox">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                    <Setter Property="FontSize" Value="11"/>
                                </Style>
                            </DataGridTextColumn.EditingElementStyle>
                        </DataGridTextColumn>
                        <!-- IsDummy -->
                        <DataGridCheckBoxColumn Header="Dummy" Width="55"
                                                Binding="{Binding IsDummy, UpdateSourceTrigger=PropertyChanged}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- 하단 힌트 + 버튼 -->
            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="TagPattern 매크로: $(F)=FlowName  $(D)=DeviceAlias  $(A)=ApiDefName"
                           FontSize="10" FontFamily="Consolas"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="확인" Style="{StaticResource DarkButton}"
                            Padding="12,4" Margin="0,0,4,0"
                            Click="Ok_Click" IsDefault="True"/>
                    <Button Content="취소" Style="{StaticResource DarkButton}"
                            Padding="12,4" IsCancel="True"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
