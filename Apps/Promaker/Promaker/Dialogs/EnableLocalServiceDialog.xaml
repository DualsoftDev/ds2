<Window x:Class="Promaker.Dialogs.EnableLocalServiceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Local 서비스 설치/재설치"
        Width="460" SizeToContent="Height"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource PrimaryTextBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,12"
                   Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11">
            <Run Text="LightHouseService 설치/재설치에 사용할 비밀번호를 입력하십시오 (PSK 와 인증서 PFX 에 공용)."/>
            <LineBreak/>
            <Run Text="기존 service 가 있으면 정지 후 재등록됩니다. 다른 머신 client 가 있다면 새 PSK 로 갱신 의무 (안전 채널로 전달)."/>
        </TextBlock>

        <TextBlock Grid.Row="1" Text="비밀번호 (PSK / Cert 공용)" FontSize="11"
                   Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,0,0,2"/>
        <PasswordBox Grid.Row="2" x:Name="PwdBox" Padding="6,4" FontFamily="Consolas"
                     Margin="0,0,0,16"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,8,0" Click="Ok_Click" IsDefault="True"/>
            <Button Content="취소" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
