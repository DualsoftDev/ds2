# 사용자별 설정 파일 (appsettings.defaults.json은 git 추적 유지)
appsettings.json
appsettings.Production.json
appsettings.Development.json
# 설치 스크립트가 기록하는 호스트 전용 포트 파일 (Program.cs 가 명시 로드)
appsettings.Hosting.json

# Node / Tailwind(stitch) CSS 빌드 — 도구/의존성은 제외.
# 산출물(wwwroot/css/stitch.css·stitch-shell.css)과 package.json/package-lock.json·tailwind*.js 는 커밋.
node_modules/
npm-install.log
npm-debug.log*
